<?php
/**
 * ==========================================================
 * JAKARTA FILM COMMISSION
 * ADMIN PANEL
 * ==========================================================
 */

session_start();

/*
|--------------------------------------------------------------------------
| CONFIGURATION
|--------------------------------------------------------------------------
*/

define('ADMIN_PATH', __DIR__);
define('BASE_URL', '/admin');

/*
|--------------------------------------------------------------------------
| AUTH CHECK
|--------------------------------------------------------------------------
*/

$isLoggedIn = isset($_SESSION['admin_login']) && $_SESSION['admin_login'] === true;

/*
|--------------------------------------------------------------------------
| REDIRECT
|--------------------------------------------------------------------------
*/

if ($isLoggedIn) {
    header("Location: dashboard.php");
    exit;
}

header("Location: login.php");
exit;
?>